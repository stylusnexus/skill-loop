{
  "name": "@stylusnexus/skill-loop-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Self-improving skills for AI coding tools",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
