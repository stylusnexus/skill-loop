{
  "name": "@stylusnexus/skill-loop-cli",
  "version": "0.3.1",
  "description": "CLI for skill-loop: inspect, amend, evaluate, and maintain AI coding tool skills",
  "bin": {
    "skill-loop": "dist/index.js",
    "skill-loop-mcp": "dist/mcp-server.js"
  },
  "files": ["dist", "skills"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stylusnexus/skill-loop": "*",
    "@modelcontextprotocol/sdk": "^1.27.0",
    "zod": "^3.24.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/stylusnexus/skill-loop.git",
    "directory": "packages/cli"
  },
  "devDependencies": {
    "vitest": "^3.1.0"
  }
}
